import React from "react";

const Card = () => {
    return (
        <div>
            <div>
                <img src={course.image.url}></img>
            </div>
        </div>
    )
}

export default Card